app_identifier(ENV.fetch("APP_IDENTIFIER", "com.aspauldingcode.Wawona"))
apple_id(ENV["APPLE_ID"]) if ENV["APPLE_ID"]
team_id(ENV["TEAM_ID"]) if ENV["TEAM_ID"]
itc_team_id(ENV["TEAM_ID"]) if ENV["TEAM_ID"]
